# Weather-app
A dark, instrument-panel style weather dashboard with animated dial gauges, an hourly trend chart, 3-day forecast, air quality index, and geolocation search. Built with vanilla HTML/CSS/JS on the WeatherAPI forecast endpoint — no framework, no build step, ready to deploy.
